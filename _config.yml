# Chatbot: set this to your deployed Cloudflare Worker URL to enable the AI chat widget.
# Leave blank (or remove the line) to disable the chatbot.
# Example: chatbot_worker_url: https://kieranmaynard-chatbot.YOUR_SUBDOMAIN.workers.dev/chat
chatbot_worker_url:

defaults:

  -

    scope:

      path: "" # an empty string here means all files in the project

    values:

      layout: "default"
